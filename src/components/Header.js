import React from 'react';
import {Image} from 'react-bootstrap';
import banner from '../../../my-news-app/src/assets/banner.jpg';
import './Header.css';

export const Header = (props) => (
    <div className={'header-content'}>
        {/*<Image src={banner} fluid/>*/}
    </div>
)
